description = "Stage 3 (diff flow): Review - Understand changes and filter diff scope."

prompt = """
Use the `estimator` skill to perform the **Review** capability.

Task: Review diff results and refine scope. {{args}}
"""
