description = "Stage 3: Metrics - Calculate complexity and estimate effort."

prompt = """
Use the `estimator` skill to perform the **Metrics** capability.

Task: Calculate metrics for In-Scope files. {{args}}
"""
